name: 🕷️ Node Crawler & Filter (Auto Update)

on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时自动运行一次
  workflow_dispatch:  # 支持手动触发

jobs:
  run-crawler:
    runs-on: ubuntu-latest

    steps:
      # Step 1: 检出仓库
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # Step 3: 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

      # Step 4: 运行爬虫脚本
      - name: Run crawler and filter nodes
        run: |
          cd .github/workflows
          python filter_crawler.py

      # Step 5: 将结果文件转为 Base64
      - name: Encode output and save as config.txt
        run: |
          cd .github/workflows
          base64 -w 0 filtered_nodes.txt > ../../config.txt

      # Step 6: 提交更新
      - name: Commit and push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add ../../config.txt .github/workflows/filtered_nodes.txt || true
          git commit -m "🤖 Auto-update filtered_nodes.txt & config.txt" || echo "No changes to commit"
          git push origin main || echo "No changes to push"
        continue-on-error: true
