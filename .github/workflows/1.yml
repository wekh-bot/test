name: 🕷️ Node Crawler & Filter (Auto Update)

on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时自动运行一次
  workflow_dispatch:  # 支持手动触发

jobs:
  run-crawler:
    runs-on: ubuntu-latest

    steps:
      # Step 1: 检出仓库
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: 设置 Python 环境
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      # Step 3: 安装依赖
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

      # Step 4: 运行爬虫脚本
      - name: Run crawler and filter nodes
        run: |
          cd .github/workflows
          python filter_crawler.py

      # Step 5: 提交并推送更新
      - name: Commit and push changes
        run: |
          echo "检查文件是否存在:"
          ls -l $GITHUB_WORKSPACE/v2ray.txt || echo "❌ 文件不存在！"

          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          
          cd $GITHUB_WORKSPACE
          git add v2ray.txt || true
          git commit -m "🤖 Auto-update v2ray.txt with latest filtered nodes" || echo "No changes to commit"
          git push origin main || echo "No changes to push"
